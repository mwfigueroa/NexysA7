# Sesión 2026-08-12 — Correcciones de XDC, Validación de I/O y Firmware ROV Principal

## Contexto
- Nexys A7-100T (xc7a100tcsg324-1) con NEORV32 RV32IMC @ 100 MHz + CFS (control ROV en hardware).
- Vivado 2026.1 (P:\AMDDesignTools\2026.1\Vivado\bin), openFPGALoader (WSL), toolchain WSL picolibc (riscv64-unknown-elf-gcc 13.2).
- ADP2230 (WaveForms bridge) para mediciones; Nexys FTDI attached a WSL (usbipd).
- Manual de referencia: `P:\NexysA7\Info\nexys-a7_rm.pdf` (descargado de digilent.com).
- Master XDC oficial: `Nexys-A7-100T-OOB/master/src/constraints/Nexys-A7-100T-Master.xdc` (GitHub Digilent).

## Logros

### 1. Firmware cfs_test corregido
- Format strings: `%02X` → `%x`, `%ld`/`%lu` → `%u`/`%d` (el printf del SDK v1.13.3 solo soporta %s %c %d %i %u %x %p).
- ALL CFS TESTS PASSED con output legible (T1-T11, PID heave 8192 exacto, depth 1033 cm, failsafe 20 ms).

### 2. Driver rov_cfs.h reescrito al ground-truth VHDL
- Base: `0xFFEB0000` (SDK v1.13.3).
- Cmd nibble en **[7:4]** (antes [3:0] — el driver viejo no funcionaba).
- **NOP (REG0=0) después de cada comando** para re-disparar el edge-detector.
- Mapa de lectura correcto: REG2=[7]armed [6]hb [5]!armed [15:8]hb_cnt [31:16]roll; REG3=pitch|yaw; REG4-6=PID0-5; REG7=depth_cm|temp.
- Heartbeat con toggle bit8; HB timeout en REG1[31:24]; presión en Pa (REG1), temp en REG2[15:0].
- API nueva: `rov_arm()`, `rov_set_hb_timeout()`, `rov_read_hb_count()`, `rov_read_depth_temp()`.

### 3. Auditoría de pines: el XDC NO correspondía a la Nexys A7-100T
Verificado contra el Master XDC oficial y el manual (pág. 25). Correcciones:
| Señal | Antes (mal) | Ahora |
|---|---|---|
| LED[15:0] | mezcla con ánodos 7-seg (J18, T14) | pines oficiales (H17…V11) |
| PWM[0..7] | VGA_HS, AUD_PWM, TMP_INT, **XA_P[4] (analógico!)**, QSPI_DQ1, accel MISO | **PMOD JA**: C17, D18, E18, G17, D17, E17, F18, G18 |
| SW[0] (gate) | R15 = switch físico SW3 | J15 = silkscreen SW0 |
| SW[1] (IRQ) | H6 = switch físico SW12 | L16 = silkscreen SW1 |
| SPI MISO | G14 (no es JB) | G16 (JB3) |
| TWI SCL/SDA | U11/U12 = ¡switches 14/13! | K1/F6 (JC1/JC2) |

- Layout PMOD corregido: **GND = pines 5 y 11; VCC = pines 6 y 12** (yo antes dije 5/6 y 11/12 — error corregido con el manual).
- Conflictos de banco 34 (1.8 V): ENC_B[4]=V9 y ENC_B[5]=U9 con LVCMOS33 daban DRC error → reubicados a F16 (JB2) y G13 (JB9). SW[8]/SW[9] = LVCMOS18 (correcto, banco 34).
- ENC_A[4]=V10 chocaba con sw15 → reubicado a D14 (JB1).
- Bitstream re-sintetizado: **WNS = +0.136 ns, WHS = +0.029 ns** (timing OK; el README viejo reportaba -2.8 ns).

### 4. Top-level VHDL (neorv32_nexys_a7.vhd)
- `SW` ampliado a 16 bits y expuesto al CPU por `gpio_i` (espejo SW→LED).
- Modo test JA: generador 1 kHz caminando por los 8 pines (1 s por pin). El servo 50 Hz original quedó comentado (pendiente restaurar).

### 5. Verificación en hardware (ADP2230)
- LED secuenciador 0→15: OK (post-corrección de pines).
- Espejo SW→LED (los 16 switches): OK.
- Walk 1 kHz en PMOD JA: **8/8 pines confirmados** (1=C17, 2=D18, 3=E18, 4=G17, 7=D17, 8=E17, 9=F18, 10=G18).

### 6. Firmware ROV principal — `neorv32/sw/rov_main/`
- Build Makefile + picolibc (mismo flujo validado que cfs_test).
- Consola interactiva (h i s a c e m d p t k n g o y f l v r x).
- Lazo fijo: heartbeat 50 ms + control 20 Hz + telemetría 2 s.
- Lazos cerrados: **depth-hold** (`o <cm>`) y **yaw-hold** (`y <hex>`) sobre el PID hardware del CFS.
- Validado: boot ARMED+HB_OK; `k 2 0x4000 0 0` + `o 100` → PID heave = **0.9999** exacto; `f` apaga todo.
- ENC3 lee 0xFFFFFFFF (sus pines están en el slot SD — pendiente).
- Driver corregido copiado a `RV32IMC/app/rov_cfs.h`; textos de main.cpp actualizados (CFS @ 0xFFEB0000).

## Estado hardware al cierre
- FPGA cargada (SRAM) con bitstream corregido + firmware rov_main corriendo (ARMED, HB OK).
- E36231A: 3.3 V alimentando la board (rail medido 2.70 V — regulador en dropout; funciona igual).
- SW0 (silkscreen) = gate PWM. PWM actualmente en modo test 1 kHz.

## Pendientes
- [ ] Restaurar modo servo 50 Hz en neorv32_nexys_a7.vhd (código original está comentado) + medir 1500 µs neutro en JA.
- [ ] Reasignar encoders: ENC3 (E2/D2 = SD_RESET/SD_DAT3), ENC5/6/7 (T9/T10/T11 = 7-seg, M18/P18 = botones) → pines libres JC (J2, G6, E7, J3, J4, E6) + JB10 (H16).
- [ ] Drivers de sensores: SPI IMU (JB) → CMD_IMU_RAW; TWI presión MS5837 (JC) → CMD_DEPTH_RAW.
- [ ] Build CMake/xPack de RV32IMC (retomar con el driver corregido ya copiado).
- [ ] Re-probar failsafe + PID con timing real en campo.

## Comandos útiles
- Build cfs_test / rov_main (WSL):
  ```
  cd /mnt/p/NexysA7/neorv32/sw/rov_main
  make NEORV32_HOME=/mnt/p/neorv32 RISCV_PREFIX=riscv64-unknown-elf- \
    USER_FLAGS='-isystem /usr/lib/picolibc/riscv64-unknown-elf/include -nostartfiles -nodefaultlibs \
    -L/usr/lib/picolibc/riscv64-unknown-elf/lib/rv32im/ilp32 -lc -lgcc -flto -msave-restore \
    -Wl,--defsym,__neorv32_rom_size=32k -Wl,--defsym,__neorv32_ram_size=8k' -j4 all
  ```
- Bitstream (Vivado batch): `vivado -mode batch -source build.tcl` desde P:\NexysA7\neorv32
- Cargar FPGA: `openFPGALoader -b nexys_a7_100 /mnt/p/NexysA7/neorv32/top.bit`
- Subir firmware: `python3 upload.py <neorv32_exe.bin>` (recargar SRAM antes si el CPU está en loop)
- ADP2230: bridge WaveForms (captura ≤5 s); el servidor pydwf se cuelga y retiene el dispositivo (matar PID si bloquea WaveForms).

## Notas de gotchas (para no repetir)
- El printf del SDK no soporta `%02X` ni `%ld` — usar helpers propios.
- El upload.py falla con "bootloader not ready" si el firmware anterior está en loop infinito → recargar SRAM primero.
- `usbipd` ADP2230 "Shared" + pydwf abierto = WaveForms.exe se cuelga en -killapp.
- Los pines 5/11 son GND y 6/12 VCC en TODOS los PMOD (no asumir simetría).
- Los switches SW8/SW9 son LVCMOS18 (banco 34) — no ponerles LVCMOS33 en el XDC.
