# Nexys A7 — Sesión del 5 de Agosto 2026

## Lo que logramos

### 1. Toolchain FPGA (WSL)
| Herramienta | Versión | Rol |
|---|---|---|
| yosys | 0.33 | Síntesis open-source |
| iverilog | 12.0 | Simulación Verilog |
| verilator | 5.020 | Simulación alto rendimiento |
| GTKWave | 3.3.116 | Visor waveforms |
| cocotb | 2.0.1 | Testbenches Python |
| fusesoc | 2.4.6 | Package manager HDL |
| graphviz | 2.43 | Visualización RTL |
| picocom | 3.1 | Terminal serial |
| LiteX | 2024.12 | SoC builder (RISC-V) |
| OpenFPGALoader | 0.12 | Programación JTAG |

### 2. Vivado 2026.1 (Windows)
- **Ubicación**: `P:\AMDDesignTools\2026.1\Vivado\`
- **Licencia**: Basic Tier, Node-Locked
- **Host ID**: `D843AE8CEFC5`
- **Cuenta AMD**: martinfigueroa447@hotmail.com
- **Expira**: 06-Ago-2027
- **Dispositivos**: Solo Artix-7 (~15 GB)

### 3. Diseño `top.v` — LED blinker + UART echo
- **Fuente**: `P:\NexysA7\src\top.v`
- **Constraints**: `P:\NexysA7\src\NexysA7.xdc`
- Reloj: 100 MHz (pin E3)
- LEDs: 16 pines mapeados
- UART: 115200 baud, 8N1
- 7-segmentos: muestra nibble alto del último byte RX

### 4. Build y programación
```
Vivado 2026.1 | XC7A100T-1CSG324 | 0 errores, 0 warnings
Timing: WNS=+6.49 ns (excelente)
Bitstream: 3.65 MB
Programación: OpenFPGALoader → 100% cargado
```

### 5. Scripts
| Archivo | Función |
|---|---|
| `scripts/build.tcl` | Síntesis + implementación + bitstream |
| `scripts/program.tcl` | Programar con Vivado Lab |
| `scripts/program.py` | Programador Python (OpenCode) |
| `scripts/program_fast.bat` | Un clic: forwardea USB + programa |

---

## Pendiente para mañana

### ⭐ Prioridad alta

1. **Verificar LED[0] parpadeando** en la Nexys A7 (~0.75 Hz)
2. **Corregir pines UART** del FTDI canal B:
   - Bajar manual de referencia: https://digilent.com/reference/programmable-logic/nexys-a7/reference-manual
   - Los pines `C4`/`D4` probablemente son incorrectos
   - Reemplazar en `NexysA7.xdc`, re-sintetizar, re-programar
3. **Probar UART echo**:
   ```bash
   wsl stty -F /dev/ttyUSB0 115200 raw
   echo "Hola" > /dev/ttyUSB0 && cat /dev/ttyUSB0
   ```

### ⭐ Prioridad media

4. **Conectar ADP2230** al pin de LED o UART para verificar señales
5. **Simular `top.v` con iverilog + GTKWave**:
   ```bash
   iverilog -o sim.vvp top.v tb.v && vvp sim.vvp
   gtkwave sim.vcd
   ```
6. **Crear testbench cocotb** para el UART echo

### ⭐ Prioridad baja

7. **Explorar LiteX** para generar un SoC RISC-V + periféricos
8. **Documentar en GitHub** el fix de UART y nuevas pruebas
9. **Probar fusesoc** para buscar cores HDL open-source

---

## Comandos rápidos

```bash
# Re-sintetizar si cambiaste el diseño
P:\AMDDesignTools\2026.1\Vivado\bin\vivado -mode batch -source P:\NexysA7\scripts\build.tcl

# Programar
wsl openFPGALoader -b nexys_a7_100 /mnt/p/NexysA7/bitstreams/top.bit

# Terminal UART (cuando funcione)
wsl picocom -b 115200 /dev/ttyUSB0
```

---

## Notas

- La licencia de Vivado vence el 6 de agosto de 2027 — renovar antes
- El Host ID de la licencia debe ser `D843AE8CEFC5` (MAC física)
- Si cambia la MAC (nuevo adaptador, dock, etc.), regenerar licencia
- OpenFPGALoader NO necesita licencia — sirve para programar sin Vivado
